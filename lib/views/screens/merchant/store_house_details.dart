import 'package:camion/data/models/user_model.dart';
import 'package:flutter/material.dart';

class StoreHouseDetails extends StatelessWidget {
  final Stores? store;
  StoreHouseDetails({Key? key, this.store}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
